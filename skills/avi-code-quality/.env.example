# avi-code-quality — local configuration
#
# Copy this file to `.env` (same directory) and set the absolute path to
# your local code-quality-analysis project (the directory containing
# assess.bb). `.env` is gitignored so your machine-specific path never
# gets committed.

CODE_QUALITY_DIR=/path/to/code-quality-analysis
